#!/usr/bin/env bash

PLUGIN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source "${PLUGIN_DIR}/scripts/colours.sh"
source "${PLUGIN_DIR}/scripts/status/status.sh"
source "${PLUGIN_DIR}/scripts/selection.sh"

